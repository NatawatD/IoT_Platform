apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: iot-infra
  labels:
    app: mongodb
spec:
  type: ClusterIP
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
      name: mongodb
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-headless
  namespace: iot-infra
  labels:
    app: mongodb
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
      name: mongodb
