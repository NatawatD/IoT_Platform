apiVersion: v1
kind: Service
metadata:
  name: iot-backend
  namespace: iot-app
  labels:
    app: iot-backend
spec:
  type: ClusterIP
  selector:
    app: iot-backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
